import { Metadata } from 'next'
import Services from '@/components/sections/services'

export const metadata: Metadata = { title: 'አገልግሎቶች | Sofi System' }

export default function ServicesPage() {
  return <Services />
}